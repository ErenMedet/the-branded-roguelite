namespace Branded.Player
{
    // What the next shot would meet on its way out: open ground, level geometry, or something it can hurt.
    public enum EAimState
    {
        Clear,
        Blocked,
        Target
    }
}
